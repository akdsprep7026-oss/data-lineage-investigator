fastapi
uvicorn[standard]
langgraph
langchain
psycopg2-binary
sqlalchemy
chromadb
langchain-google-genai
# Alternate LLM provider, selected with LLM_PROVIDER=groq. Useful for
# dev/test iterations so Gemini's small daily free-tier quota can be
# saved for evaluation runs. See app/graph/llm.py.
langchain-groq
pydantic
python-dotenv
pytest
alembic

# Self-contained local Postgres binary (no server install required) used
# for local dev/testing when DATABASE_URL isn't pointed at a real Postgres.
pgserver
